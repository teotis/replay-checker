# 任务：修复 README 中的拼写错误

这是一个示例 case，用于展示 replay_checker 的 case 结构。

## 背景

项目根目录下的 `README.md` 文件中有一处拼写错误："recieve" 应为 "receive"。

## 任务

1. 找到并修复 `README.md` 中的拼写错误
2. 提交修复

## 输出要求

- 修改后的文件 diff
- 完成报告（completion_report.md）
- 验证命令输出（evidence.yaml）
